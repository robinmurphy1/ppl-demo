name: dvt
runtime:
  name: python
  options:
    virtualenv: venv
description: A minimal AWS Python Pulumi program
main: ./s3-auto-bucket/__main__.py
config:
  pulumi:tags:
    value:
      pulumi:template: aws-python

